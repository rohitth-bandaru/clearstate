# TEMPLATE: Docker Compose reads .env only, NOT this file.
# 1. Copy this file to .env in this same folder (repo root):
#      cp .env.example .env
# 2. Edit .env if needed. Then run: docker-compose up --build
#
# Required for Docker: set JWT_SECRET (and optionally DB_PASSWORD).

# MySQL root password (backend and mysql container)
DB_PASSWORD=clearstatus

# Backend JWT signing secret (required for auth)
JWT_SECRET=0rilvn1v2JV1NE9JnQ4fzH7eEC7CfAEkzYuZhrsS1FM=

# Optional: Google OAuth (for sign-in with Google)
GOOGLE_CLIENT_ID=397196918261-rcbmej8l6c6aug6b45s0q6ht5n3tf38d.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=397196918261-rcbmej8l6c6aug6b45s0q6ht5n3tf38d.apps.googleusercontent.com

# Optional: API URL the browser uses (default works for local Docker)
NEXT_PUBLIC_API_URL=http://localhost:8080
